function soma (x, y) {
    const resultado = x + y;
    return resultado;
}

const mostrarResult = soma(4 , 2);
console.log(mostrarResult);


// outra maneira

const raiz = function(x, y) {
    return x + y;
}
console.log(raiz(9, 8));
